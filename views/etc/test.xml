<?xml version="1.0"?>
<view-definition xmlns="http://unact.net/xml/xi" name="test" label="Тестовая штука">
    <view-schema>
        <form name="sysuser">
            <parameter name="name">
                <init with="username"/>
            </parameter>
            <form name="cat-file" new-only="true" extendable="true">
                <!--form name="ppsterminal" label="Терминал" choise-style="table" >
                    <parameter name="searcher" type="string" editable="true" label="ИД или часть адреса"/>
                    <field name="id" label="ИД"/>
                    <field name="name"/>
                    <field name="address" editable="true"/>
                    <field name="lastconnect"/>
                </form-->
                <field name="xid" />
                <field name="code" editable="true" />
                <field name="data" editable="true" />
            </form>
        </form>
    </view-schema>
    <workflow>
        <step>
            <display>
                <input form="cat-file" field="*"/>
            </display>
        </step>
    </workflow>
</view-definition>
